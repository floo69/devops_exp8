version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies
  build:
    commands:
      - echo Build started
      - mkdir build
      - cp -r * build/
artifacts:
  files:
    - '**/*'
  base-directory: build